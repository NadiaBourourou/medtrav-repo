package tests.FlightServices;

import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

import delegates.FlightServicesDelegate;
import entities.Flight;
import entities.FlightMatching;

public class TestFindAllArrivals {

	public static void main(String[] args) {
		
	/*	List<Str> departures = FlightServicesDelegate.doFindAllDepartures();
		List<FlightMatching> departuresFiltree = new ArrayList<FlightMatching>();

		System.out.println(departures.size() + " departures \n");

		 for (int it=0; it<departures.size(); it++) {
		

			 FlightMatching o = departures.get(it);
	            if (!departuresFiltree.contains(o))//pr enlever les doublons
	      

	            	departuresFiltree.add(o);
	        }

		 System.out.println(departuresFiltree.size() + " departuresFiltrees \n");*/
		 
	/*	Iterator i = departuresFiltree.iterator();
		while(i.hasNext()){
		 String x = (String) i.next(); //La méthode next() renvoie un objet de type Object.Il faut donc caster le résultat avec le type correspondant.
		System.out.println(x);
	*/	 //on obtient ainsi les departures des aéroports étrangers triés par ordre alphabétique

	/*	 for(FlightMatching f:departuresFiltree)
			{System.out.println(f.getDeparture());}
	*/	 
		}
		
		
		

	}


